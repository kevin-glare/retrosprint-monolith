<%= turbo_frame_tag dom_id(bad) do %>
  <input id="sticker-<%= bad.id %>" class="form-control sticker-content" autocomplete="off" type="text" name="sticker[content]" value="<%= bad.content %>">
<% end %>